# AI Guideline – Landing Page Portfolio

Este documento define regras e padrões que a IA deve seguir ao gerar código ou sugerir melhorias para o projeto **Landing Page Portfolio** feito com **Next.js 15 + TailwindCSS**.

---

## 📂 Estrutura do Projeto

- O projeto usa **App Router** (`/app`).
- Componentes ficam em `/components`.
- Estilos globais em `/app/globals.css`.
- Reutilizar o máximo possível de componentes (Buttons, Cards, Sections).
- Seguir **arquitetura limpa e modular**.

---

## 🎨 Estilo & Design

- Usar **TailwindCSS** para toda a estilização (sem CSS puro, exceto ajustes globais).
- Paleta de cores (dark mode): Utilizar cores ja existentes
- Usar **animações suaves** com [Framer Motion](https://www.framer.com/motion/) quando aplicável. (Algumas animações ja criadas em src/components/animations) reaproveite se adequado

---

## 🛠️ Boas Práticas de Código

- Utilizar **TypeScript**.
- Usar **Server Components** sempre que possível, **Client Components** apenas quando necessário (`"use client"`).
- Nomear componentes dessa forma: exampleExample.
- Evitar lógica duplicada → extrair hooks/utilitários em `/hooks` or `/utils`
- Usar `async/await` em vez de `.then()`.
- Usar **ESLint + Prettier** (padrão Next.js).
- Estruturar imports: libs externas → internos → estilos.

---

## 🔍 Acessibilidade & SEO

- Usar tags semânticas (`<header>`, `<main>`, `<section>`, `<footer>`).
- Todas as imagens devem ter `alt`.
- Usar **Next.js Image** (`next/image`) sempre que possível.
- Incluir meta tags de SEO em `/app/layout.tsx`.
- Usar `aria-` attributes quando necessário.

---

## ⚡ Performance

- Sempre otimizar imagens com `next/image`.
- Carregar fontes via `next/font` (ex: `Inter`).
- Usar **Lazy loading** em imagens e seções pesadas.
- Manter código enxuto (evitar dependências desnecessárias).

---

## 🧪 Testes

- Testes com **Vitest + React Testing Library**.
- Testar:
  - Renderização correta do componente.
  - Acessibilidade (se possível).
  - Interações principais (botões, navegação).
